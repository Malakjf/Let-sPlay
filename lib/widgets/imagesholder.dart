import 'package:flutter/material.dart';

class ImagesHolder extends StatelessWidget {
  const ImagesHolder({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
